services:
  - type: web
    name: transit-ai
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /api/buses
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
